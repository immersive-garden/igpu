/// <reference types="vite/client" />
// Vite client types provide `import.meta.env`/`import.meta.hot` and the
// `*?raw` (and friends) module declarations used for shader text imports.
// Engine runtime globals live in ambient.d.ts.
